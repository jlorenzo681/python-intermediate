# Exercise 20: SQLite Database Operations
# Objective: Perform CRUD operations with SQLite

# Hints:
# - import sqlite3
# - sqlite3.connect('database.db') to connect
# - cursor.execute(sql, params) for queries
# - Use ? for parameters (prevents SQL injection)
# - conn.commit() to save changes
# - cursor.fetchall() to get results
# - Always close connection or use 'with'

# Your code here:
