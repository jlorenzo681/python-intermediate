# Exercise 17: Working with Datetime
# Objective: Work with dates, times, and time deltas

# Hints:
# - from datetime import datetime, date, timedelta
# - datetime.now() for current datetime
# - strftime(format) to format dates
# - strptime(string, format) to parse dates
# - timedelta(days=7) for date arithmetic
# - Common formats: %Y (year), %m (month), %d (day), %H (hour), %M (minute)

# Your code here:
